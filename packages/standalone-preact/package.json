{
  "name": "@gi-tcg/standalone-preact",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "NODE_ENV=production bunx --bun vite build",
    "preview": "NODE_ENV=production bunx --bun vite preview"
  },
  "dependencies": {
    "preact": "^10.19.3",
    "@gi-tcg/webui": "workspace:*"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.10"
  }
}
