{
  "name": "@jenshin-tcg/data",
  "type": "module",
  "scripts": {
    "build:entry": "node ./scripts/generate_imports.mjs",
    "build:rollup": "rollup -c",
    "build:types": "dts-bundle-generator src/index.ts -o dist/index.d.ts",
    "build": "pnpm build:entry && pnpm build:rollup && pnpm build:types"
  },
  "dependencies": {
    "@jenshin-tcg/typings": "workspace:*"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.1.0",
    "@rollup/plugin-typescript": "^11.1.1",
    "dts-bundle-generator": "^8.0.1",
    "glob": "^10.2.7",
    "rollup": "^3.25.0",
    "tslib": "^2.5.3",
    "typescript": "^5.1.3"
  }
}
