{
  "name": "genius-invokation",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "//": "Waiting for --recursive flag, see https://github.com/oven-sh/bun/issues/5350",
  "scripts": {
    "build:typings": "cd packages/typings && bun run build",
    "build:frontend": "cd packages/standalone && bun run build",
    "build:all": "bun build:typings && bun build:frontend"
  }
}
